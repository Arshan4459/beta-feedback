[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"

# Netlify auto-detects Next.js; pinning the adapter keeps builds reproducible.
[[plugins]]
  package = "@netlify/plugin-nextjs"
